{
  "name": "mobility-core",
  "version": "0.1.0",
  "private": true,
  "description": "Trotxi platform monorepo — repo-wide tooling (formatting, git hooks).",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm --filter @trotxi/api test",
    "test:e2e": "pnpm --filter @trotxi/e2e test",
    "test:e2e:ui": "pnpm --filter @trotxi/e2e run test:ui",
    "coverage": "pnpm --filter @trotxi/api run test:coverage",
    "check": "pnpm run format:check && pnpm run typecheck && pnpm run lint && pnpm test",
    "api": "pnpm --filter @trotxi/api dev",
    "migrate": "pnpm --filter @trotxi/api run migrate",
    "start": "pnpm -r --parallel --if-present run dev",
    "infra:up": "docker compose -f infra/docker/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker/docker-compose.yml down",
    "infra:logs": "docker compose -f infra/docker/docker-compose.yml logs -f",
    "infra:ps": "docker compose -f infra/docker/docker-compose.yml ps",
    "codegen": "openapi-generator-cli generate -i https://trotxi-api-staging.onrender.com/docs/json -g dart-dio -o apps/api_client --additional-properties=pubName=trotxi_api_client,pubAuthor=LFE"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.40.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.1.0",
    "prettier": "^3.9.6"
  }
}
