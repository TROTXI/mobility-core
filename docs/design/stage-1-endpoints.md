# Stage 1: complete baseline endpoint inventory

Generated by scripts/build-contract.mjs from the locally built API OpenAPI, not staging or grep. Baseline source: 43cdae0. 103 documented operations; 143 proposed target operations. Target-only operations appear in target.openapi.json. Identifier mappings are semantic: a route ID is not a geometry/version ID. Follow the new resource links, never substitute old IDs blindly.

| Current                                          | Replacement                                                       | Consumer       | Decision                 |
| ------------------------------------------------ | ----------------------------------------------------------------- | -------------- | ------------------------ |
| `DELETE /me`                                     | `DELETE /v1/me`                                                   | both apps      | replace; remove old path |
| `DELETE /me/sessions/{id}`                       | `DELETE /v1/me/sessions/{id}`                                     | rider/catalog  | replace; remove old path |
| `GET /`                                          | `GET /`                                                           | infrastructure | keep stable boundary     |
| `GET /admin/commute-requests`                    | `GET /v1/ops/commute-requests`                                    | ops/scripts    | replace; remove old path |
| `GET /admin/commute-requests/{id}/events`        | `GET /v1/ops/commute-requests/{id}/events`                        | ops/scripts    | replace; remove old path |
| `GET /admin/commute-slots`                       | `GET /v1/ops/commute-slots`                                       | ops/scripts    | replace; remove old path |
| `GET /admin/driver-requests`                     | `GET /v1/ops/driver-requests`                                     | ops/scripts    | replace; remove old path |
| `GET /admin/drivers`                             | `GET /v1/ops/drivers`                                             | ops/scripts    | replace; remove old path |
| `GET /admin/flags`                               | `GET /v1/ops/flags`                                               | ops/scripts    | replace; remove old path |
| `GET /admin/incidents`                           | `GET /v1/ops/incidents`                                           | ops/scripts    | replace; remove old path |
| `GET /admin/min-versions`                        | `GET /v1/ops/min-versions`                                        | ops/scripts    | replace; remove old path |
| `GET /admin/payments/reviews`                    | `GET /v1/ops/payments/reviews`                                    | ops/scripts    | replace; remove old path |
| `GET /admin/plan-pricing`                        | `GET /v1/ops/plan-pricing`                                        | ops/scripts    | replace; remove old path |
| `GET /admin/routes`                              | `GET /v1/ops/routes`                                              | ops/scripts    | replace; remove old path |
| `GET /admin/routes/{id}/fares`                   | `GET /v1/ops/routes/{id}/fares`                                   | ops/scripts    | replace; remove old path |
| `GET /admin/stops`                               | `GET /v1/ops/stops`                                               | ops/scripts    | replace; remove old path |
| `GET /admin/trips`                               | `GET /v1/ops/trips`                                               | ops/scripts    | replace; remove old path |
| `GET /admin/vehicles`                            | `GET /v1/ops/vehicles`                                            | ops/scripts    | replace; remove old path |
| `GET /boarding/manifest`                         | `GET /v1/driver/trips/{id}/manifest`                              | driver         | replace; remove old path |
| `GET /flags`                                     | `GET /flags`                                                      | both apps      | keep stable boundary     |
| `GET /healthz`                                   | `GET /healthz`                                                    | infrastructure | keep stable boundary     |
| `GET /me`                                        | `GET /v1/me`                                                      | both apps      | replace; remove old path |
| `GET /me/avatar`                                 | `GET /v1/me/avatar`                                               | both apps      | replace; remove old path |
| `GET /me/commute-requests`                       | `GET /v1/me/commute-requests`                                     | rider/catalog  | replace; remove old path |
| `GET /me/incidents`                              | `GET /v1/driver/incidents`                                        | driver         | replace; remove old path |
| `GET /me/pass`                                   | `POST /v1/me/reservations/{id}/pass`                              | rider/catalog  | replace; remove old path |
| `GET /me/reservations`                           | `GET /v1/me/reservations`                                         | rider/catalog  | replace; remove old path |
| `GET /me/rides`                                  | `GET /v1/me/membership`                                           | rider/catalog  | replace; remove old path |
| `GET /me/sessions`                               | `GET /v1/me/sessions`                                             | rider/catalog  | replace; remove old path |
| `GET /me/subscription`                           | `GET /v1/me/membership`                                           | rider/catalog  | replace; remove old path |
| `GET /me/trips`                                  | `GET /v1/driver/trips`                                            | driver         | replace; remove old path |
| `GET /me/work/requests`                          | `GET /v1/driver/requests`                                         | driver         | replace; remove old path |
| `GET /me/work/routes`                            | `GET /v1/driver/available-routes`                                 | driver         | replace; remove old path |
| `GET /readyz`                                    | `GET /readyz`                                                     | infrastructure | keep stable boundary     |
| `GET /routes`                                    | `GET /v1/routes`                                                  | rider/catalog  | replace; remove old path |
| `GET /routes/{id}`                               | `GET /v1/routes/{id}`                                             | rider/catalog  | replace; remove old path |
| `GET /routes/{id}/geometry`                      | `GET /v1/route-geometries/{id}`                                   | rider/catalog  | replace; remove old path |
| `GET /trips`                                     | `GET /v1/trips`                                                   | rider/catalog  | replace; remove old path |
| `GET /trips/{id}`                                | `GET /v1/trips/{id}`                                              | rider/catalog  | replace; remove old path |
| `GET /trips/{id}/position`                       | `GET /v1/trips/{id}/live`                                         | rider/catalog  | replace; remove old path |
| `GET /trips/{id}/summary`                        | `GET /v1/driver/trips/{id}/summary`                               | driver         | replace; remove old path |
| `GET /version`                                   | `GET /version`                                                    | infrastructure | keep stable boundary     |
| `PATCH /admin/driver-requests/{id}`              | `POST /v1/ops/driver-requests/{id}/decisions`                     | ops/scripts    | replace; remove old path |
| `PATCH /admin/drivers/{id}`                      | `PATCH /v1/ops/drivers/{id}`                                      | ops/scripts    | replace; remove old path |
| `PATCH /admin/drivers/{id}/credentials`          | `POST /v1/ops/drivers/{id}/credentials/actions`                   | ops/scripts    | replace; remove old path |
| `PATCH /admin/incidents/{id}`                    | `POST /v1/ops/incidents/{id}/decisions`                           | ops/scripts    | replace; remove old path |
| `PATCH /admin/plan-pricing/{plan}`               | `PATCH /v1/ops/plan-pricing/{plan}`                               | ops/scripts    | replace; remove old path |
| `PATCH /admin/routes/{id}`                       | `PATCH /v1/ops/routes/{id}`                                       | ops/scripts    | replace; remove old path |
| `PATCH /admin/stops/{id}`                        | `PATCH /v1/ops/stops/{id}`                                        | ops/scripts    | replace; remove old path |
| `PATCH /admin/trips/{id}`                        | `PATCH /v1/ops/trips/{id}`                                        | ops/scripts    | replace; remove old path |
| `PATCH /admin/users/{id}/role`                   | `PATCH /v1/ops/users/{id}/role`                                   | ops/scripts    | replace; remove old path |
| `PATCH /admin/vehicles/{id}`                     | `PATCH /v1/ops/vehicles/{id}`                                     | ops/scripts    | replace; remove old path |
| `PATCH /me`                                      | `PATCH /v1/me`                                                    | both apps      | replace; remove old path |
| `POST /admin/ask-dispatch`                       | `POST /v1/ops/maintenance/ask-dispatch`                           | ops/scripts    | replace; remove old path |
| `POST /admin/close-subscription-periods`         | `POST /v1/ops/maintenance/period-close`                           | ops/scripts    | replace; remove old path |
| `POST /admin/commute-requests/{id}/decision`     | `POST /v1/ops/commute-requests/{id}/decisions`                    | ops/scripts    | replace; remove old path |
| `POST /admin/commute-slots`                      | `POST /v1/ops/commute-slots`                                      | ops/scripts    | replace; remove old path |
| `POST /admin/commute-slots/{id}/retire`          | `POST /v1/ops/commute-slots/{id}/retire`                          | ops/scripts    | replace; remove old path |
| `POST /admin/convert-credits`                    | `POST /v1/ops/maintenance/period-close`                           | ops/scripts    | replace; remove old path |
| `POST /admin/drivers`                            | `POST /v1/ops/drivers`                                            | ops/scripts    | replace; remove old path |
| `POST /admin/drivers/{id}/credentials`           | `POST /v1/ops/drivers/{id}/credentials`                           | ops/scripts    | replace; remove old path |
| `POST /admin/drivers/{id}/credentials/reset-pin` | `POST /v1/ops/drivers/{id}/credentials/reset-pin`                 | ops/scripts    | replace; remove old path |
| `POST /admin/expire-subscriptions`               | `POST /v1/ops/maintenance/period-close`                           | ops/scripts    | replace; remove old path |
| `POST /admin/learn-routes`                       | `POST /v1/ops/maintenance/route-learning`                         | ops/scripts    | replace; remove old path |
| `POST /admin/payments/maintenance`               | `POST /v1/ops/maintenance/payments`                               | ops/scripts    | replace; remove old path |
| `POST /admin/payments/process-webhooks`          | `POST /v1/ops/maintenance/payment-inbox`                          | ops/scripts    | replace; remove old path |
| `POST /admin/payments/reconcile`                 | `POST /v1/ops/maintenance/payment-reconciliation`                 | ops/scripts    | replace; remove old path |
| `POST /admin/resolve-defaults`                   | `POST /v1/ops/maintenance/reservation-defaults`                   | ops/scripts    | replace; remove old path |
| `POST /admin/resolve-no-shows`                   | `POST /v1/ops/maintenance/no-shows`                               | ops/scripts    | replace; remove old path |
| `POST /admin/routes`                             | `POST /v1/ops/routes`                                             | ops/scripts    | replace; remove old path |
| `POST /admin/routes/{id}/stops`                  | `POST /v1/ops/route-patterns/{id}/versions`                       | ops/scripts    | replace; remove old path |
| `POST /admin/stops`                              | `POST /v1/ops/stops`                                              | ops/scripts    | replace; remove old path |
| `POST /admin/trips`                              | `POST /v1/ops/trips`                                              | ops/scripts    | replace; remove old path |
| `POST /admin/vehicles`                           | `POST /v1/ops/vehicles`                                           | ops/scripts    | replace; remove old path |
| `POST /auth/apple`                               | `POST /v1/auth/apple`                                             | both apps      | replace; remove old path |
| `POST /auth/driver`                              | `POST /v1/auth/driver`                                            | both apps      | replace; remove old path |
| `POST /auth/driver/pin`                          | `POST /v1/auth/driver/pin`                                        | both apps      | replace; remove old path |
| `POST /auth/google`                              | `POST /v1/auth/google`                                            | both apps      | replace; remove old path |
| `POST /auth/logout`                              | `POST /v1/auth/logout`                                            | both apps      | replace; remove old path |
| `POST /auth/refresh`                             | `POST /v1/auth/refresh`                                           | both apps      | replace; remove old path |
| `POST /boarding/board`                           | `POST /v1/driver/trips/{id}/boardings`                            | driver         | replace; remove old path |
| `POST /boarding/no-show`                         | `POST /v1/driver/trips/{id}/reservations/{reservationId}/no-show` | driver         | replace; remove old path |
| `POST /boarding/scan`                            | `POST /v1/driver/trips/{id}/boardings`                            | driver         | replace; remove old path |
| `POST /boarding/verify-code`                     | `POST /v1/driver/trips/{id}/boardings`                            | driver         | replace; remove old path |
| `POST /boarding/verify-pin`                      | `POST /v1/driver/trips/{id}/boardings`                            | driver         | replace; remove old path |
| `POST /me/avatar`                                | `PUT /v1/me/avatar`                                               | both apps      | replace; remove old path |
| `POST /me/commute-requests`                      | `POST /v1/me/commute-requests`                                    | rider/catalog  | replace; remove old path |
| `POST /me/commute-requests/{id}/withdraw`        | `POST /v1/me/commute-requests/{id}/withdraw`                      | rider/catalog  | replace; remove old path |
| `POST /me/devices`                               | `POST /v1/me/devices`                                             | both apps      | replace; remove old path |
| `POST /me/incidents`                             | `POST /v1/driver/incidents`                                       | driver         | replace; remove old path |
| `POST /me/reservations`                          | `POST /v1/me/reservation-decisions`                               | rider/catalog  | replace; remove old path |
| `POST /me/work/requests`                         | `POST /v1/driver/requests`                                        | driver         | replace; remove old path |
| `POST /me/work/requests/{id}/withdraw`           | `POST /v1/driver/requests/{id}/withdraw`                          | driver         | replace; remove old path |
| `POST /payments/subscribe`                       | `POST /v1/me/purchases`                                           | rider/catalog  | replace; remove old path |
| `POST /trips/{id}/arrive`                        | `POST /v1/driver/trips/{id}/arrivals`                             | driver         | replace; remove old path |
| `POST /trips/{id}/complete`                      | `POST /v1/driver/trips/{id}/complete`                             | driver         | replace; remove old path |
| `POST /trips/{id}/position`                      | `POST /v1/driver/trips/{id}/positions`                            | rider/catalog  | replace; remove old path |
| `POST /trips/{id}/start`                         | `POST /v1/driver/trips/{id}/start`                                | driver         | replace; remove old path |
| `POST /webhooks/paystack`                        | `POST /webhooks/paystack`                                         | Paystack       | keep stable boundary     |
| `PUT /admin/flags/{key}`                         | `PUT /v1/ops/flags/{key}`                                         | ops/scripts    | replace; remove old path |
| `PUT /admin/min-versions/{platform}`             | `PUT /v1/ops/min-versions/{app}/{platform}`                       | ops/scripts    | replace; remove old path |
| `PUT /admin/routes/{id}/fare`                    | `POST /v1/ops/routes/{id}/fares`                                  | ops/scripts    | replace; remove old path |
| `PUT /admin/trips/{id}/assignment`               | `PUT /v1/ops/trips/{id}/assignment`                               | ops/scripts    | replace; remove old path |

## Infrastructure outside generated business OpenAPI

- GET /metrics: retain separate operational endpoint with metrics authentication; never expose in mobile client.
- GET /docs/json and Swagger UI/static asset routes under /docs: retain documentation service; publish only the target spec after cutover.
- Automatically generated HEAD routes and CORS OPTIONS follow their GET/resource registration; not separate business operations.
- Public bootstrap GET /flags remains stable and unversioned. Its redesigned shape is a deliberate prelaunch replacement; future evolution is additive with old-client parsing tests.
- No legacy alias survives cutover. Health/version payload changes must update deployment probes/consumers as part of the release.
