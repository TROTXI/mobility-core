// Exact existing test names are evidence locators, not claims of full coverage.
export const invariants = [
  [
    'COM-01',
    'commute.pg',
    'serializes duplicate submissions and leaves current assignment untouched',
    'One open transfer request per rider; submission does not change the effective assignment.',
    'PG reduced schema',
  ],
  [
    'COM-02',
    'commute.pg',
    'only one of two riders can hold the last recurring slot',
    'Concurrent approvals cannot allocate the same recurring slot twice.',
    'PG reduced schema; no lock-overlap instrumentation',
  ],
  [
    'COM-03',
    'commute.pg',
    'requires consent, preserves rides and paid time, skips dispatch and expiry while paused',
    'Pause requires consent; no dispatch/close while paused; resume extends coverage by elapsed pause and preserves rides.',
    'PG reduced schema',
  ],
  [
    'COM-04',
    'commute.pg',
    'applies once, cancels future old bookings, preserves period pricing and enforces new route/time',
    'Apply once; cancel future old bookings; preserve purchase price/history; enforce new assignment route, stops and time.',
    'PG reduced schema + migration 043',
  ],
  [
    'COM-05',
    'commute.pg',
    'blocks unsettled trips and different fares without partial changes',
    'Unsettled service and different-fare transfers block application without partial changes; no invented proration.',
    'PG reduced schema',
  ],
  [
    'COM-06',
    'commute.pg',
    'expiry/refund closes pauses and approvals and releases held capacity',
    'Ending membership releases pending transfer capacity and closes pauses.',
    'Fixture tests status=expired; actual refund path not exercised by this test despite its title',
  ],
  [
    'COM-07',
    'commute.pg',
    'erasure scrubs rider notes, closes requests and releases held slots',
    'Erasure hides requests, scrubs rider/decision notes and releases held slots.',
    'PG reduced schema',
  ],
  [
    'COM-08',
    'commute.pg',
    'rejects unauthorized decisions, reversed stops and past dates',
    'Cannot self-approve, withdraw another rider request or submit reversed stops/past date.',
    'PG reduced schema',
  ],
  [
    'COM-09',
    'commute.pg',
    'HTTP roles, validation, durable submit, rider isolation and ops decision work together',
    'HTTP caller scope and ops decisions use the durable service, not client-supplied ownership.',
    'Fastify + PG reduced schema',
  ],
  [
    'RES-01',
    'reservations',
    'respond upserts: confirm → reserved, decline → declined, re-answer overwrites',
    'One rider/date/direction intent can be answered again; neither answer invents a second reservation.',
    'Memory',
  ],
  [
    'RES-02',
    'seat-capacity',
    'refuses a rider once the vehicle is full',
    'Capacity limits confirmed seats; re-confirmation must not count the same seat twice.',
    'Memory, not PG contention proof',
  ],
  [
    'RES-03',
    'seat-capacity',
    'never refuses a decline',
    'Decline remains possible without entitlement/capacity; it releases a held seat.',
    'Memory',
  ],
  [
    'RES-04',
    'seat-capacity',
    'marks the riders it could not seat `unseated`, not `pending` (#210)',
    'Default-yes seats only available capacity; overflow is explicitly unseated and not chargeable as a no-show.',
    'Memory',
  ],
  [
    'RES-05',
    'ask-dispatch',
    'is idempotent — re-running does not double-seed or double-notify a rider',
    'Dispatch creates at most one pending intent per rider/date/direction. Notifications are best-effort and may repeat.',
    'Title overstates: assertion allows repeated notifications',
  ],
  [
    'BRD-01',
    'boarding.deduction',
    'a valid scan of a confirmed rider boards the seat and debits one ride',
    'A valid confirmed boarding consumes exactly one ride.',
    'Memory',
  ],
  [
    'BRD-02',
    'boarding.deduction',
    're-scanning a boarded rider (fresh QR) does not double-charge',
    'A fresh QR for an already charged reservation does not charge again.',
    'Memory',
  ],
  [
    'BRD-03',
    'boarding.manifest-actions',
    'lets a rider who ran up late still board, at no extra cost',
    'Late boarding after a charged no-show changes attendance without a second debit.',
    'Memory',
  ],
  [
    'BRD-04',
    'no-show',
    'only touches still-reserved rows of that day+direction',
    'No-show sweep charges only still-reserved intended scope; not pending, declined, boarded or another direction.',
    'Memory',
  ],
  [
    'BRD-05',
    'boarding.code-only',
    'will not reach a seat on another run',
    'Code proof is restricted to assigned trip and reservation; knowing another code grants no authority.',
    'HTTP + memory',
  ],
  [
    'BRD-06',
    'boarding.manifest',
    'rejects a driver who is not the trip’s assigned driver (403)',
    'Manifest is available only to assigned driver, not every driver-role account.',
    'HTTP + memory',
  ],
  [
    'BRD-07',
    'boarding.routes',
    'an access token cannot be reused as a boarding pass (audience separation)',
    'Pass signature, expiry and audience are distinct from API authentication.',
    'HTTP + memory',
  ],
  [
    'BRD-08',
    'boarding.routes',
    'a KV outage does not block boarding (single-use check fails open)',
    'Existing availability rule tolerates replay-cache failure, but durable reservation charge remains unique.',
    'Memory; this test proves scan result, not real database failure atomicity',
  ],
  [
    'BRD-09',
    'boarding.routes',
    'an audit-write failure does not block the scan result (fails open)',
    'Auxiliary scan-audit failure must not invalidate an otherwise durable boarding.',
    'Memory; target must keep financial write failures distinct',
  ],
  [
    'ID-01',
    'auth.service',
    'reuses the same user on repeat sign-in with the same identity',
    'Provider+subject selects the same user on repeat sign-in.',
    'Memory',
  ],
  [
    'ID-02',
    'auth.apple',
    'treats the same subject id from Apple and Google as two different people',
    'Provider namespace is part of identity; do not merge accounts by matching subject/email alone.',
    'Memory',
  ],
  [
    'ID-03',
    'auth.service',
    'reuse detection: replaying a rotated token revokes the whole session family',
    'Rotate refresh credentials and reject consumed-token reuse with session-family revocation.',
    'Memory; preserves current behavior, lost-response recovery needs explicit test',
  ],
  [
    'ID-04',
    'auth.service',
    "logout does not trigger reuse-revocation of the user's other sessions",
    'Logout must not revoke an unrelated device session.',
    'Memory',
  ],
  [
    'ID-05',
    'auth.sessions',
    "cannot revoke another user's session",
    'Session lists/revocations are self-scoped and disclose no refresh secrets.',
    'HTTP + memory',
  ],
  [
    'ID-06',
    'driver-auth',
    'answers the same 401 for an unknown code and a wrong PIN',
    'Driver authentication does not disclose credential existence; lockout follows bounded failures.',
    'HTTP + memory',
  ],
  [
    'ID-07',
    'driver-auth',
    'refuses a suspended credential and kills its live sessions',
    'Suspended driver credentials cannot refresh/sign in through an existing session.',
    'HTTP + memory',
  ],
  [
    'ID-08',
    'driver-auth',
    'shortens the session when the handset is not the driver’s own',
    'Shared-device choice changes refresh lifetime, not role or assignment scope.',
    'HTTP + memory',
  ],
  [
    'ID-09',
    'account-deletion',
    'keeps the user row so the money tables stay attributable',
    'Erasure hides/scrubs identity without deleting accountable financial history.',
    'Memory; not a full privacy audit',
  ],
  [
    'ID-10',
    'account-deletion',
    'removes the avatar from object storage, not just the column',
    'Erasure removes avatar object, provider linkage and push tokens; retries remain safe.',
    'Fake object store and memory; separate tests cover linkage/devices',
  ],
  [
    'TRP-01',
    'trip-lifecycle',
    'moves a run scheduled -> active -> completed and stamps both times',
    'Trip transitions and timestamps are coherent; cannot complete unstarted or restart completed trips.',
    'Memory',
  ],
  [
    'TRP-02',
    'driver-schedule-and-progress',
    'still shows nobody another driver’s schedule',
    'Driver schedule scope survives range filtering and pagination.',
    'HTTP + memory',
  ],
  [
    'TRP-03',
    'driver-schedule-and-progress',
    'lets a driver tap back after overshooting',
    'Explicit progress correction may move backward; do not confuse with stale replay.',
    'HTTP + memory',
  ],
  [
    'TRP-04',
    'driver-schedule-and-progress',
    'refuses a stop that is not on the route',
    'Reported progress must be a stop on the trip route; target uses immutable occurrence ID.',
    'HTTP + memory; version ownership is target-only',
  ],
  [
    'TRP-05',
    'trip-progress.pg',
    'upgrades the old constraint, preserves rows, accepts zero and rejects negatives',
    'Zero is valid existing stop sequence; storage refactor must not shift the first stop.',
    'PG migration constraint',
  ],
  [
    'GPS-01',
    'trip-position.pg',
    'stores delayed fixes once and keeps the newest captured fix live',
    'Duplicate fix preserves original payload/receipt; out-of-order older capture does not replace newest; inactive trip rejects write.',
    'Real PG adapter; max:1 serial pool, not concurrent GPS worker proof',
  ],
  [
    'GPS-02',
    'positions.routes',
    'rejects a driver who is not the assigned driver with 403',
    'Only the current assigned driver can publish trip position.',
    'HTTP + memory',
  ],
  [
    'GPS-03',
    'positions.routes',
    'does not expose a cached fix after a trip completes',
    'Inactive trip must not leak a formerly cached position.',
    'HTTP + memory',
  ],
  [
    'GPS-04',
    'route-learning.service',
    'learns morning and evening separately, never pooled',
    'Directional speed observations are not averaged together.',
    'Memory; explicit pattern direction replaces time-of-day inference',
  ],
  [
    'GPS-05',
    'segment-speed.aggregate',
    'ignores segments with too few observations',
    'Observed ETA speed needs three valid segment observations per direction, not just three trips.',
    'Pure computation',
  ],
  [
    'GPS-06',
    'segment-speed.aggregate',
    'discards implausible speeds rather than letting them skew a median',
    'Invalid timing/speed observations cannot skew learned speed; use explicit fallback when insufficient.',
    'Pure computation',
  ],
  [
    'GPS-07',
    'route-geometry',
    'considers only the five most recent runs',
    'Learning candidate selection is bounded; five candidates is not a convergence guarantee.',
    'Pure computation',
  ],
  [
    'OPS-01',
    'driver-incidents',
    'refuses a driver attaching a report to a run that is not theirs',
    'Incident provenance is derived from assigned trip; own lists stay isolated.',
    'HTTP + memory',
  ],
  [
    'OPS-02',
    'driver-incidents',
    'accepts a report with no trip — a fault found in the yard still matters',
    'Pre-trip incident reporting remains possible; optional location is driver-supplied, never commuter GPS.',
    'HTTP + memory',
  ],
  [
    'OPS-03',
    'driver-work-requests',
    'approving a route change does not reassign anything',
    'Driver request approval is a decision, not automatic fleet reassignment.',
    'HTTP + memory',
  ],
  [
    'OPS-04',
    'driver-work-requests',
    'refuses a second decision rather than overwriting the first',
    'Decisions remain attributable and cannot be overwritten by a repeat ops action.',
    'HTTP + memory',
  ],
  [
    'OPS-05',
    'driver-work-requests',
    'does not let one driver withdraw another’s, or tell them it exists',
    'Only the requesting driver can withdraw an undecided request; do not disclose others.',
    'HTTP + memory',
  ],
];
