name: trotxi_client
version: 1.0.0
description: Handwritten client layer on top of the generated trotxi_api_client.
publish_to: none

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  dio: '^5.7.0'
  trotxi_api_client:
    path: ../api_client

dev_dependencies:
  test: ^1.24.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
  http_mock_adapter: ^0.6.1
