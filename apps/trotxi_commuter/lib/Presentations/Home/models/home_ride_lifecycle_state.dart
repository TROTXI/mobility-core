enum HomeRideLifecycleState {
  tomorrowConfirmation,
  rideConfirmed,
  vehicleApproaching,
  readyToBoard,
  inTrip,
  tripCompleted,
  expandedLiveMap,
}

enum HomeQuickAction {
  trackRide,
  schedule,
  wallet,
  support,
  shareTrip,
  changeRide,
  liveMap,
  pickupInfo,
  pickup,
  tripCode,
  tripDetails,
}

enum CommuterDestination { home, trips, wallet, profile }
